EAX * 5 = EAX + EAX * 4 (Очень умный типо)

mov EAX, [EBX + 2 * ESI] (записываем в EAX число AX)
mov EAX, [label], label == const, const == 0x1234 (пример)
xchg EAX, ECX (меняем местами)

Есть арифметика (в презентации): есть кстати еще флаги, кроме основных четырех:
флаг четности (ну и еще какие-то, больше не помню)

1110 SHR 0111
     SAR 1111 ; какой-то левый пример

DX:AX = AX * src16 - число представлено как пара регистров
EDX:EAX = EAX * src32 - аналогично
